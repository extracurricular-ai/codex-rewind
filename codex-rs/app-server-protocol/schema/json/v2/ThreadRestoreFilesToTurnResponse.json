{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "summary": {
      "description": "Null when the thread is not tracking snapshots or the turn has none, which callers should report rather than treat as success.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "title": "ThreadRestoreFilesToTurnResponse",
  "type": "object"
}